<h1>All your applications</h1>
<!-- <ul>
  <% @candidatures.each do |candidature| %>
  <li>
    <p><%=  candidature.url_flat.flat.name %></p>
    <p><%=  candidature.url_flat.flat.nb_room %></p>
    <p><%=  candidature.url_flat.flat.address %></p>
    <p><%=  candidature.url_flat.flat.price %></p>
  </li>
  <% end %>
</ul> -->
<div class="row">
  <% @candidatures.each do |candidature| %>
    <div class="col-xs-12 col-md-6">
      <%= render 'shared/flat_card', candidature: candidature %>
    </div>
  <% end %>
</div>
